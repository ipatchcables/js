/*
Loop through an object's properties in arbitrary order
Loop over an obje3ct
*/

const users = {1: "Sally", 2: "Billy", 3: "Ashley", 4: "Timmy"};

for (let prop in users){
 if (users[prop] === "Sally") { //Remove sally and replace with unkown
   users[prop] = "Unkown";
 }
  console.log(users[prop]);
}


const drinks = ["coffe", "Sprite"];
drinks.forEach((d) => {
  console.log(d);
}) ;
